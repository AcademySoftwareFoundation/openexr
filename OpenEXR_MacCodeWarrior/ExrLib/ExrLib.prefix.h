// prefix file for building the OpenEXR library with CodeWarrior#define PLATFORM_DARWIN_PPC 1// this seems to be necessary for Mach-O builds in CodeWarrior#if __MACH__#include <MSLCarbonPrefix.h>#endif// size_t: needed for ImfMisc.h.  This can be removed if ImfMisc.h is// changed to #include <size_t.h>, or the appropriate cross-platform// header file.#include <size_t.h>using std::size_t;// M_PI: non-standard definition for PI#define	M_PI		3.14159265358979323846